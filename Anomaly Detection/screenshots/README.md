# Screenshots

## before/

The "before" state is captured in the five screenshots supplied in the
2026-09-04 kickoff message:

1. Pump station, 3 analog inputs — `19MIS24848` / 203 Saluda 3
2. Pump station, 2 analog inputs — `19MIS25555` / 106 Hwy 86 3
3. Pump station, 1 analog input — `19MIS25559` / 113 Wren Middle School
4. Flow monitor — site `40480` / ReWa 600A-354
5. Explorer tab — topic list

These are the reference for every layout ticket. Note in all three pump station
shots: the back button spans the full page width, and the header reads
`Serial: <id>` on the left with the description pushed to the far right — the two
issues EXPLORER-004/007 address.

**Action needed:** save those five images into this directory as
`before/pump-3-analog.png`, `before/pump-2-analog.png`, `before/pump-1-analog.png`,
`before/flow-monitor.png`, `before/explorer.png`. They arrived as chat attachments
and were not written to disk.

## after/

To be captured once the app is running against the broker. Capture the same five
states, plus dark mode and window-resize variants for the layout tickets.
